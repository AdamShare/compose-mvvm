package com.share.sample.integrations.main

/**
 * Application-level scope.
 *
 * Dependencies scoped with @SingleIn(AppScope::class) are cached
 * for the lifetime of the SampleApplicationGraph instance.
 */
object AppScope
